Hello
This is today's first coding practice.
```cpp
int maxPerformance(int n, vector<int>& sp, vector<int>& eff, int k) {
        vector<pair<int, int>> es(n);
        for (int i=0; i<n; i++){
            es.push_back({eff[i], sp[i]});
        }
        sort(es.rbegin(), es.rend());
        long long totSpeed = 0;
        long long ans = 0;
        priority_queue<int, vector<int>, greater<int>> pq;
        for (int i=0; i<n; i++){
            totSpeed += es[i].second;
            pq.push(es[i].second);
            if (pq.size()>k){
                totSpeed -= pq.top();
                pq.pop();
            }
            ans = max(ans, totSpeed*es[i].first);
        }
        return ans % (1000000007);
    }

```
